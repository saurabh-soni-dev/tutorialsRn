import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    conatiner: {
        // flex: 1
    },
    errorText: {
        fontSize: 14,
        color: 'red', 
        fontWeight: '400'
    }
}) 